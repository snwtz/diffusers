# Minimal Requirements for Multispectral VAE Training
# Only essential dependencies for core functionality

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0

# Data processing
numpy>=1.24.0
Pillow>=10.0.0
rasterio>=1.3.0  # Required for multispectral TIFF files

# Metrics and evaluation
scikit-image>=0.19.0  # For SSIM
torchmetrics>=1.0.0  # For SpectralAngleMapper

# Progress tracking
tqdm>=4.65.0

# Optional but recommended
wandb>=0.16.0  # For experiment tracking
huggingface_hub>=0.19.0  # For model management 